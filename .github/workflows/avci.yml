name: 🦅 Avci Operasyonu

on:
  schedule:
    - cron: '0 */6 * * *'
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  av-zamani:
    runs-on: ubuntu-latest

    steps:
      - name: 📂 Depoyu Kontrol Et
        uses: actions/checkout@v3

      - name: 🐍 Python Sistemini Hazırla
        # HATA BURADAYDI: Başına 'actions/' eklendi
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 📦 Gerekli Araçları Kur
        run: pip install requests

      - name: 🏹 Avcıyı Sahaya Gönder
        run: python avci.py

      - name: 🏆 Ganimetleri Kaydet
        run: |
          git config --global user.name 'Avci-Bot'
          git config --global user.email 'avci@bot.com'
          git add avci_listesi.m3u
          git commit -m "🦅 Ganimetler Güncellendi: $(date +'%d-%m-%Y %H:%M')" || echo "Yeni ganimet yok."
          git push
